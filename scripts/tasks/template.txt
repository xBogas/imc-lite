// Core includes
#include "Tasks.h"
#include "Timer.h"

namespace Task {
namespace _param_task_name_ {

static struct Task: public AbstractTask
{
  Task():
    AbstractTask("_param_task_name_")
  {
    debug("Creating task");
    timer = setTimer(DEFAULT_TIMER);
    if (timer != NULL)
      debug("Timer allocated");
    else
      debug("Failed to allocate timer");
  }

  ~Task()
  { }

  void setup(void)
  { }

  //! Main loop.
  void loop()
  {
    debug("Task running");
  }
} worker;


}}

TASK_EXPORT(_param_task_name_)